apiVersion: v1
kind: Service
metadata:
  name: smallest-spring-app-svc
spec:
  selector:
    app: smallest-spring-app
    harness.io/color: blue
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: NodePort
